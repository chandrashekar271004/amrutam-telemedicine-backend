import {Router} from 'express'; import {requireAuth,requireRole} from '../../middleware/auth.js'; import {validate} from '../../middleware/validate.js'; import {Role} from '@prisma/client'; import * as c from './controller.js'; import {createDoctorSchema,doctorSearchSchema} from './schema.js';
const r=Router(); r.get('/',validate(doctorSearchSchema),c.search); r.post('/',requireAuth,requireRole(Role.ADMIN),validate(createDoctorSchema),c.create); r.post('/:id/verify',requireAuth,requireRole(Role.ADMIN),c.verify); export default r;
