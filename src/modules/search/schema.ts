import {z} from 'zod'; export const searchSchema=z.object({body:z.object({}),params:z.object({}),query:z.object({q:z.string().max(100).optional(),specialty:z.string().max(100).optional(),fromFee:z.coerce.number().nonnegative().optional(),toFee:z.coerce.number().nonnegative().optional(),page:z.coerce.number().int().min(1).default(1),limit:z.coerce.number().int().min(1).max(50).default(20)})});
